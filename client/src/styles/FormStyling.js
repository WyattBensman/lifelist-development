import { StyleSheet } from "react-native";

export const formStyling = StyleSheet.create({
  input: {
    flex: 1,
    height: 40,
    marginBottom: 12,
    borderWidth: 1,
    padding: 10,
    borderRadius: 8,
    borderColor: "#D4D4D4",
  },
});
