import { StyleSheet } from "react-native";

export const formStyles = StyleSheet.create({
  input: {
    height: 36,
    borderWidth: 1,
    padding: 10,
    borderRadius: 8,
    borderColor: "#D4D4D4",
  },
});
